-- The core of project
-- import options: to put lines, numbers, tabs... You can see in lua/config/options
require("config.options")

-- import keymaps: to put shortcuts in keyboard to access features
require("config.keymaps")
